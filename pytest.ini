[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
addopts = -ra
testpaths = tests
markers =
    live: reaches the public internet to verify stealth against fingerprinting sites (opt-in via RUN_LIVE_TESTS=1)
