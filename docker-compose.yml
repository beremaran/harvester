services:
  renderer-worker:
    build:
      context: .
    platform: linux/amd64
    init: true
    shm_size: 1gb
    ports:
      - "${PORT:-8082}:${PORT:-8082}"
    environment:
      PORT: "${PORT:-8082}"
      RENDER_CONCURRENCY: "${RENDER_CONCURRENCY:-3}"
